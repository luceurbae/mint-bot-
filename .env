RPC_URL=https://rpc.ankr.com/eth
CONTRACT_ADDRESS=0xYOUR_CONTRACT_ADDRESS
MINT_FUNC=mint
ABI_OVERRIDE=

# (Optional) Set a specific mint time in UTC. The bot will wait until this time.
# Format: YYYY-MM-DDTHH:mm:ssZ (Example: 2025-12-25T10:00:00Z for 10 AM UTC)
# Leave this section blank if you want the bot to start working immediately.
MINT_DATETIME_UTC=

# === Mint Parameters ===
MINT_PRICE=0.03
MINT_AMOUNT=1
GAS_LIMIT=300000
GAS_PRICE_GWEI=

# === Mode ===
# simple  -> minimal snippet (single wallet)
# single  -> advanced single wallet (retry, gas bump)
# multi   -> advanced multi-wallet
MODE=simple


# === Wallet Configuration ===
# For MODE=simple/single, use PRIVATE_KEY
# For MODE=multi, use PRIVATE_KEYS (comma-separated)
PRIVATE_KEY=0xYOUR_PRIVATE_KEY         
PRIVATE_KEYS=0xKEY1,0xKEY2,0xKEY3            
TX_DELAY_MS=2500

# === Retry / Backoff ===
# Number of attempts to retry a transaction if it fails
# Backoff time in milliseconds before retrying
RETRY_ATTEMPTS=5
RETRY_BACKOFF_MS=2000
RETRY_BACKOFF_MULTIPLIER=1.6

# === Gas bump during retry ===
GAS_BUMP_PERCENT=15

# === (Optional) EIP-1559 override ===
MAX_FEE_GWEI=
MAX_PRIORITY_GWEI=